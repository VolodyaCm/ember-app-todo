import Route from '@ember/routing/route';

export default Route.extend({
  afterModel() {
    const g_id = 'g_00000000001';
    const sg_id = 'sg_0000000001';
    const controller = this.controllerFor('todo');
    const group = controller.Gr
  }
});
